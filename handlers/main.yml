---
# HAProxy and Vault service management is owned by the sol1.haproxy / sol1.hashivault
# roles respectively; rustguac only manages its own service here.
- name: restart rustguac
  ansible.builtin.systemd:
    name: rustguac
    state: restarted
