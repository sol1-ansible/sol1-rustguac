---
galaxy_info:
  author: Sol1
  description: Install and configure rustguac remote access gateway with optional HAProxy and Vault
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - trixie
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - guacamole
    - rdp
    - ssh
    - vnc
    - remote-access
# sol1-haproxy and sol1-hashivault are consumed conditionally via include_role
# (only when rustguac_haproxy_enabled / rustguac_vault_install are set), so they are
# intentionally NOT listed as hard dependencies here. Ensure both roles are available
# on the roles path — see requirements.yml.
dependencies: []
