---
- name: Deploy config.toml
  ansible.builtin.template:
    src: config.toml.j2
    dest: "{{ rustguac_prefix }}/config.toml"
    owner: rustguac
    group: rustguac
    mode: "0640"
  notify: restart rustguac
